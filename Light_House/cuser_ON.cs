using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class cuser_ON : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
                Cursor.visible = true;//마우스 커서 보이기
        }

    // Update is called once per frame
    void Update()
    {
        
    }
}
